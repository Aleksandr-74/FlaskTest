function Fractions(numerator, denominator){
    this.numerator =  +prompt ('Введите числитель');
    this.denominator = +prompt ('Введите знаменатель');
}


//сложение
function addition(a, b){
        if(a.denominator = b.denominator){
           let numerator = a.numerator + b.numerator;
            return `${numerator} | ${a.denominator};`;
        }
        for(let i = 0; a.denominator != b.denominator; ++i){
            
        }
        if(a.denominator < b.denominator){

        }


    }

//вычитание    
function subtraction(){
        if(a.denominator = b.denominator){
            let numerator = a.numerator - b.numerator;
             return `${numerator} | ${a.denominator};`;
         }

    }
//умножение     
    this.humiliation = function(){

    }
//деление    
    this.division = function(){

    }
//сокращение    
    this.reduction = function(){

    }





